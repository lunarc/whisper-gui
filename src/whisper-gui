#!/usr/bin/env bash
# Bash wrapper for whisper-gui (development mode)
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PYTHONPATH="$SCRIPT_DIR" exec python3 "$SCRIPT_DIR/whisper-gui.py" "$@"
